# Custom-AI-Chatbot-with-Memory
Custom AI Chatbot with Memory — a terminal-based Python chatbot powered by a local LLM (Ollama) that actually remembers your conversation. Features structured session history, input validation, a FIFO sliding-window algorithm to prevent token overflow, and local persistence so memory survives a restart. No API key, no billing.
